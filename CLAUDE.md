# 동물 친구 보물 찾기 — 포팅 작업 규칙

이 프로젝트는 초등 과학 PPT(VBA) 게임을 단일 HTML 웹앱으로 **충실히 이식**하는 작업이다.
아래 규칙은 실제 작업 중 발견한 오류에서 나온 것이다. **새 화면·기능을 만들 때 매번 적용한다.**

## 절대 원칙

1. **눈대중 금지 — 원본 좌표를 추출해서 쓴다.**
   슬라이드는 12192000×6858000 EMU = 정확히 1280×720 px. `EMU / 9525 = px` 로 무대 좌표와 1:1 대응한다.
   - 좌표·크기·회전: `docs/slide-coordinates.json` 또는 `python3 tools/extract_slide.py <N> --layout`
   - 색상: 원본 도형의 `srgbClr`/`schemeClr` 를 그대로 읽는다. `tx1`=검정, `bg1`=흰색, `bg2`=회색.
     **추정하지 않는다.** (배지 색을 갈색으로 잘못 넣었던 사례가 있음 → 실제는 tx1/bg1)

2. **원본 에셋을 재사용한다. 단, 매핑을 반드시 확인한다.**
   `assets/original/imageNN.png` 는 번호와 내용이 직관적으로 맞지 않는다.
   - 곰=image15, 토끼=image16 (image6/7 은 텍스트 배너였음), 흙더미=image17, i-Scream 로고=image19
   - 새 에셋을 쓰기 전 **Read 로 실제 그림을 눈으로 확인**하고, 크기(`PIL`)도 점검한다.

3. **폰트는 강원교육튼튼.** `webapp/assets/GangwonEduTunTun.otf` 를 `@font-face` 로 임베드.

## 반복해서 나온 오류 (같은 실수 금지)

- **한글 줄바꿈:** 무대에 `word-break:keep-all` 이 없으면 `직접`이 `직/접`으로 쪼개진다. 이미 무대에 적용되어 있으니 유지한다.
- **이미지 속 글자 세로 중앙:** 이미지에 여백/장식이 있으면 이미지 박스 중앙 ≠ 시각 중앙.
  예) 나무 간판(image21)은 위쪽 ~43%가 사슬 → 판자 영역 기준으로 중앙 정렬.
- **버튼의 흰 바탕:** `background-image` PNG 버튼은 버튼 기본 배경색이 투명 여백으로 비친다.
  전역 규칙 `button { background-color:transparent }` 로 막아 두었으니, 새 버튼도 이 규칙에 의존한다.
- **레이어 순서:** 전경(보석·캐릭터·아이템)은 배경 소품(흙더미)보다 **위 z-index**. 안 그러면 잘린다.
- **상호작용 충실도:** 원본 메커니즘을 그대로. 예) 땅파기는 **흙더미 하나씩**, **이미지 위 클릭**에서만 작동
  (화면 아무 데나 클릭 → 전부 올라오는 식으로 만들지 않는다).

## 문제은행 (questions.json)

- 원본 문제은행은 **슬라이드 70부터, 문항당 3슬라이드**(표지/문제/정답및해설). `((마지막+1)-70)/3` 문항.
- **네 가지 유형**이 섞여 있다. 단답형만 가정하면 정답이 어긋난다:
  - `short` 단답형 — 정답 115pt
  - `ox` — 정답 슬라이드의 `o`/`x` 도형 중 **hidden 이 아닌 쪽**이 정답
  - `blank` 빈칸 — 문장에 정답이 그대로 있고 `?` 도형이 덮어 가림.
    추출할 때 `[[?n]]`(n=가린 글자 수) 자리표시로 바꾸고, 화면에서는 원본과 같은
    **흰 네모칸 + 분홍(#EB3D7B) `?`** 로 그린다. `( 온도 )` 처럼 괄호가 있으면 괄호째 가린다.
  - `choice` 4지선다 — 번호 배지(24pt)+보기(36pt) 4쌍. 배지는 **검정 배경/흰 글자**
- 재생성: `python3 tools/dump_questions.py`. 이 스크립트는 **정답 누락·정답 노출을 자동 점검**한다.
- 유형마다 문제 영역 좌표가 다르다(`Q_BOX`). 공용 좌표를 쓰면 보기와 겹친다.

## 작업 루틴 (화면 하나 만들 때마다)

1. 원본 좌표·색상·텍스트를 추출한다 (`tools/extract_slide.py`).
2. 필요한 에셋을 `assets/original/` 에서 확인 후 `webapp/assets/` 로 복사.
3. 화면·로직을 `webapp/index.html` 에 추가. 매크로 로직은 원본 동작 그대로 옮긴다.
4. **브라우저 검증** (개발 서버 + Browser 도구):
   - 무대 스케일을 정규화해 요소의 **무대 기준 좌표가 추출값과 일치**하는지 측정 (오차 ±1px)
   - `read_console_messages` 로 에러 0 확인
   - 스크린샷으로 눈으로 확인
   - 이식한 매크로 동작을 실제로 실행해 결과 검증
5. README 진행표 갱신 → 커밋 → `git push origin main`. 커밋 메시지에 무엇을·왜 고쳤는지 남긴다.

## 실행 / 저장소

- 실행: `cd webapp && python3 -m http.server 8000` → http://localhost:8000
- 원본 `.pptm` 은 상용 배포물이라 저장소에 넣지 않는다. 좌표는 `docs/slide-coordinates.json` 에 있다.
- `_media/`, `_slides/` 는 `.pptm` 에서 재생성되는 작업 폴더 → 커밋하지 않는다.
